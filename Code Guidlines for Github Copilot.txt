Guideline 1
: Use the same coding style as the existing codebase.  Readonly variables must begin with an underscore (_).

Guideline 2
: Use the same naming conventions as the existing codebase.  For example, use camelCase for variables and PascalCase for classes.

Guideline 3
: Use the same formatting conventions as the existing codebase.  For example, use spaces instead of tabs.

Guideline 4
:  Tools used:  Newtonsoft.Json, XUnit, Moq, AutoFixture, NLog, FluentAssertions, AutoMapper, EntityFrameworkCore